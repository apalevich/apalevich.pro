# Endpoint the /contact form POSTs to (Formspree, Web3Forms, or your own API).
# Receives JSON: { name, contact, website, message }
# If unset, the form posts to /api/contact (which will 404 on the static site).
PUBLIC_CONTACT_ENDPOINT=https://your-endpoint.example/contact
