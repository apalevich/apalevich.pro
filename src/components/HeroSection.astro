---
import { Picture } from 'astro:assets';
import heroImage from '../images/hero_image.jpg';
import Highlight1 from '../images/Highlight1.svg'
import Highlight2 from '../images/Highlight2.svg'
---
<section
    class="pt-24 md:mt-0 md:h-screen flex flex-col justify-center text-center md:text-left md:flex-row md:justify-between md:items-center lg:px-48 md:px-12 px-4 bg-secondary">
    <div class="md:flex-1 md:mr-10">
      <h1 class="font-pt-serif text-5xl font-bold mb-7 capitalize">
        Web development with
        <span class="bg-underline1 bg-left-bottom bg-no-repeat bg-100%">
            results
        </span>
        at the forefront
      </h1>
      <p class="font-pt-serif font-normal mb-7">
        We deliver clear and measurable projects where quality is more important than hours.
      </p>
      <div class="font-montserrat">
        <button class="bg-black px-6 py-4 rounded-lg border-2 border-black border-solid text-white mr-2 mb-2">
            Let's talk
        </button>
        <button class="px-6 py-4 border-2 border-black border-solid rounded-lg text-black">
            View Study Cases
        </button>
      </div>
    </div>
    <div class="flex justify-around md:block mt-8 md:mt-0 md:flex-1">
        <div class="relative">
            <Picture
                src={Highlight1}
                format='svg'alt="Hightlight decoration"
                class='absolute -top-24 left-12'    
            />
        </div>
        <Picture
            formats={['webp', 'jpg']}
            src={heroImage}
            alt="Artem Palevich works at computer"
            class="rotate-6 mx-auto shadow-xl"
            width={heroImage.width/2} height={heroImage.height/2}
        />
        <div class="relative">
            <Picture
                src={Highlight2}
                format='svg'
                alt="Hightlight decoration"
                class='absolute -bottom-16 right-16'
            />
        </div>
    </div>
  </section>